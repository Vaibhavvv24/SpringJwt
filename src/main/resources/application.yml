server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_jwt
    username: root
    password: Cricket@2345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  security:
    user:
      password: spring